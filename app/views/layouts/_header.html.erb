<span class="primary-logo">
</span>
<span id="navbar">
  <% if user_signed_in? %>
    <span><a href="/", class="btn btn-primary">Index</a></span>
    <span><%= link_to 'Settings', edit_user_registration_path, class: "btn btn-primary" %></span>
    <span><%= link_to 'Shop', 'https://www.dartagnan.com/', class: "btn btn-primary" %></span>
    <span><%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn btn-primary" %></span>
    <% if current_user.email == 'admin' %>
      <span><%= link_to 'Sign up', new_user_registration_path, class: "btn btn-primary" %></span>
    <% end %>
  <% else %>
    <span><%= link_to 'Sign in', new_user_session_path, class: "btn btn-primary" %></span>
  <% end %>
</span>
<h1 class="title">Cookbook
</h1>
<% if user_signed_in? %>
  <span>Signed in as <%= current_user.name %></span>
  <%= image_tag user_avatar(current_user, 30), class: "img-circle" %>
<% else %>
  <span>No user currently signed in</span>
<% end %>
<%= content_tag(:div, flash[:error], :id => "flash_error") if flash[:error] %>
<%= content_tag(:div, flash[:notice], :id => "flash_notice") if flash[:notice] %>
<%= content_tag(:div, flash[:alert], :id => "flash_alert") if flash[:alert] %>
<br>
